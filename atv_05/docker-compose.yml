version: '3.8'

services:
  server:
    build: .
    command: python server.py
    ports:
      - "5000:5000"

  client:
    build: .
    command: python client.py
    environment:
      - SERVER_HOST=server
    depends_on:
      - server